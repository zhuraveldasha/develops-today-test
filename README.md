DevelopsToday Test
==================

# Ініціалізація реакт застосунку.

```shell
npm create vite@latest develops-today-test-app -- --template react-ts

npm install

npx storybook@latest init
```

```shell
npm run dev
npm run storybook
```
